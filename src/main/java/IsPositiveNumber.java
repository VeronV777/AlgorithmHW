public class IsPositiveNumber {
    //Напишите алгоритм который возвращает true, если численный
    // параметр больше или равен нулю, и возвращает false, если параметр меньше 0.
    public boolean IsPositiveNumberAlgorithm (int first){
        if (first>=0){
            return true;

        } else
            return false;}}

